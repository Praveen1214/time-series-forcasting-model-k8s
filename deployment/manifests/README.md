# Kubernetes Manifests

Placeholder for Kubernetes deployment manifests.

## Contents

- `prediction-service.yaml` - Deployment for the prediction service
- `service.yaml` - Service exposing the prediction API
- `configmap.yaml` - Configuration for the model
- `secrets.yaml` - Secrets for sensitive data (not committed)

These will be created when deploying the model.
