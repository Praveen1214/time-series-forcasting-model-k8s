# ── FastAPI & Server ─────────────────────────────────────────
fastapi>=0.104.0,<1.0.0
uvicorn[standard]>=0.24.0,<1.0.0
pydantic>=2.0.0,<3.0.0

# ── ML Inference (CPU-only, much smaller than full TF) ──────
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0
tensorflow-cpu>=2.15.0,<2.17.0
joblib>=1.3.0

# ── Production Server ──────────────────────────────────────
gunicorn>=21.0.0,<23.0.0
